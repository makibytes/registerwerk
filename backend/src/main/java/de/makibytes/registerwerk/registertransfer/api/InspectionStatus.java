package de.makibytes.registerwerk.registertransfer.api;

/** Lifecycle of a §10 eWpG register inspection request. */
public enum InspectionStatus {
    REQUESTED,
    APPROVED,
    REJECTED,
    FULFILLED
}
