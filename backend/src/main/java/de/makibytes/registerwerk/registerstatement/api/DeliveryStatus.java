package de.makibytes.registerwerk.registerstatement.api;

/** Delivery state of a register statement issued in text form (§19 eWpG). */
public enum DeliveryStatus {
    PENDING,
    DELIVERED,
    FAILED
}
