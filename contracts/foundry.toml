[profile.default]
src = "src"
out = "out"
test = "test"
script = "script"
libs = ["lib"]
# Explicit rather than left to solc/forge's own default: "osaka" (Fusaka's execution-layer
# fork) is the newest EVM version this toolchain (forge 1.5.1) recognizes — there is no later
# fork to target yet. When a newer fork's opcodes/semantics are supported by a forge/solc
# upgrade, bump this explicitly and re-run `forge test` in full before relying on it; do not
# let it silently drift with the toolchain's own default.
evm_version = "osaka"
# Required for EwpgPasskeyAccount (WebAuthn/P256 signature parsing hits "stack too deep"
# without it) — matches the vendored openzeppelin-contracts lib's own default settings.
optimizer = true
optimizer_runs = 200
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "@openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/",
    "forge-std/=lib/forge-std/src/",
    "@erc3643/=lib/erc3643/contracts/",
    "@onchain-id/solidity/=lib/onchain-id/",
    "@fhevm/=lib/fhevm/"
]
fs_permissions = [
    { access = "read-write", path = "./" },
    { access = "write", path = "/output" }
]

[rpc_endpoints]
fhenix_helium = "${FHENIX_HELIUM_RPC}"
inco_rivest   = "${INCO_RIVEST_RPC}"

# Used by contracts/test/lending/EwpgRepoMarket.invariant.t.sol (finding #12, Phase 7) — the
# repo markets are the first contracts in this codebase to move real pooled stablecoin/collateral
# value with nontrivial internal accounting (scaled shares/debt, a bad-debt write-off), so a
# fuzzed handler run is worth the extra CI time relative to the ordinary unit-test suite.
[invariant]
runs = 256
depth = 20
fail_on_revert = false
